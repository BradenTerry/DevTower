node_modules/
out/
*.vsix
.DS_Store

# bundled webview build output (regenerated by `npm run build`; vsix packaging
# runs it via the vscode:prepublish script, so it never needs to be committed)
media/crew.js
media/crew.js.map
.devtower/
.fleet/

# screenshot harness (throwaway captures)
screenshots/out/
screenshots/.harness.html
test-results/
